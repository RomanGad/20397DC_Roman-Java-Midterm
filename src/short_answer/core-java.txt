***ANSWER ALL OF THE QUESTIONS BELOW***

What is a programming language?
Answer: A programming language is a vocabulary and set of grammatical rules
        for instructing a computer or computing device to perform specific tasks.

What is a JDK composed of? Define each components?
Answer:

What is an IDE?
Answer:IDE is a software application/program that enables the user to write a Java program.

What are some of the most popular Java IDEs available in the market?
Answer:IntelliJ IDEA, Eclipse and NetBeans are the most popular IDEs.

Explain the entire java life cycle.
Answer:

What is a class?
Answer:A class is a group of object which have common properties,
       and it is a template/blueprint that describes the behavior/state that the object of its type support.

What is an object?
Answer: An object is an entity that has state and behavior, and it is an instance of a class

What is the entry point of a Java project?
Answer: The main method.

Why is the main() method static?
Answer: A main method is a special method that Java looks for  when running a program. It is the entry point of Java code and
        can be called by using the class name only without creating an object of a class, so that compiler can call it
         without the creation of an object or before the creation of an object of the class.

Which class is the superclass of all classes?
Answer: Object class.

What is difference between path and classpath variables?
Answer:

What is the difference between an Interface and Abstract class?
Answer:

Can you make an Object from an Interface and Abstract class? If yes or no, how would you do it?
Answer:

What is Access Modifier?
Answer:Access modifiers specifies the accessibility or scope of a field, method, constructor or class.

What is OOP? Define each OOP concept.
Answer: OOP is stands for Object-Oriented Programming. OOP is a programming concept that works on the principal of Encapsulation
        Polymorphism, Inheritance and Abstraction.
        Encapsulation is a concept of wrapping data and code, the variables of a class are always hidden from other classes.
                      It can only be accessed using the methods of their current class.
        Polymorphism is
        Inheritance- in this concept one object acquires the properties and behaviors of the parent object.
                     It’s creating a parent-child relationship between two classes.
        Abstraction is an act of representing essential features without including background details.

What is Java Package and which package is imported by default?
Answer: Java package is a container of a group of similar types of classes, interfaces and sub-packages.
        java.lang package imports internally by default. It provides the fundamental classes that are necessary to design a basic
        java programming.

What is an API? List some API's that you have used so far.
Answer:

Does java support multiple inheritance? Why or why not?
Answer: Java Doesn't support multiple inheritance, because handling the complexity that causes due to multiple inheritances
        is very complex and  the reason behind this is to prevent ambiguity.

What is method overloading? List all of the different ways to overload.
Answer: Method Overloading is a feature that allows a class to have more than one method having the same name,
        if their argument lists are different.
        There are three types of method overriding: Number of parameters, Sequence of Data type of parameters and
        Data type of parameter.

Explain exceptions in java and how to handle them.
Answer:

What is static keyword? When and how do we use it?
Answer:The Static keyword is used for memory management mainly. We can apply static keyword with variable, methods, blocks and
       nested classes. We can use Static keywords We can declare a class static by using the static keyword.

What is final keyword? When and how do we use it?
Answer: Using a final keyword makes the attributes, method or class constant(can't be extended). A final method cannot
        be overridden.

Explain the difference between final, finally and finalize?
Answer:

What is a constructor?
Answer: A constructor initializes an object when it is created. The constructor is called when an object of a class is created.

Can we have multiple constructors in a class?
Answer:

If we don't have a constructor declared, what is called during the object creation?
Answer: Default constructor.

What is "this" keyword in Java? When would we use it?
Answer: The "this" keyword can be used to refer current class instance variable. We use "this" Keyword
        to eliminate the confusion between class attributes and parameters with the same name.

What is "super" keyword in Java? What are some scenarios we would use it?
Answer: A "super" keyword in Java is a reference variable which is used to refer immediate parent class object.
        We would use "this" keyword to access the data members of parent class when both parent and child class have member with same name.
        and to access the method of parent class when child class has overridden that method.

What does JVM stand for?
Answer: JVM stands for Java virtual machine.

Is JVM platform independent?
Answer: NO, JVM is platform-dependent.

What version of Java are you using?
Answer: I am using  Java JDK 11 version.

What does JAR stand for?
Answer: JAR stands for Java Archive.

What is the difference between JDK and JVM?
Answer:

What is the difference between JVM and JRE?
Answer:

Explain the difference between compile-time and run-time?
Answer: Compile-time error is the error occurs during code writing syntax whereas run-time error is the error that
        occurs during running of the code.

What is heap?
Answer: Heap is the area of memory used to store objects instantiated by application running on the JVM.

How does Java manage its memory?
Answer: Java does memory management automatically. Java uses an automatic memory management system called a garbage collector.

What is the difference between String, StringBuffer and StringBuilder?
Answer:

What is Singleton class?
Answer: A singleton class is a class that allows only one instance to be created and provides global access to all other classes
        through this single object or instance.

What is Serialization and Deserialization?
Answer:

Explain the difference between While and Do..While loops?
Answer: While loop is used to iterate a part of the program several times when the number of iteration is not know, the loop keeps on
        iterating till the condition returns a false value however in do while loop if the number of iteration is not fixed and we
        must have to execute the loop at least once because condition is checked after loop body.

What is Enum?
Answer:

What is Iterator?
Answer:

Which one will take more memory: an int or Integer? Why?
Answer:

Why are Strings considered to be immutable in Java?
Answer:

What is constructor chaining in Java?
Answer:

The difference between Serial and Parallel Garbage Collector?
Answer:

What does JIT stands for?
Answer:

Explain the process of Java garbage collection?
Answer:

Can you guarantee the garbage collection process?
Answer:

What is the difference between stack and heap in Java?
Answer:

What is reflection in Java and why is it useful?
Answer:

What is multithreading in Java?
Answer:

What is synchronization in Java?
Answer:

What is a Framework?
Answer:

What are the testing Frameworks available in Java?
Answer:

What are jUnit and testNG?
Answer:

What are the dependencies for this project?
Answer:

What is dependency injection in Java?
Answer:

Explain what static binding and dynamic binding is?
Answer: